# 本地音频测试数据集

真实音频统一保存在工程根目录的 `.local-test-data/audio/`。该目录已被 Git 整体忽略，音频、清单和转写结果都不能提交到仓库。

数据集清单使用 `manifest.json`，每个样本记录稳定 ID、相对文件名、字节数、SHA-256、MIME 类型、标签和最小验收条件。新功能可以从 `scripts/local-audio-dataset.mjs` 导入加载器，按样本 ID 或标签选择测试数据。

本地检查只会验证文件大小、SHA-256、媒体元数据和 Git 忽略状态，不会上传音频或调用任何 Provider：

```bash
npm run verify:audio-dataset
npm run verify:audio-dataset -- regression-422
```

`.local-test-data` 不得用作真实 Provider 验收、转写回归或任何网络请求的输入。需要验证转写主链时，使用脚本生成的合成音频和本机 HTTP mock。

新增样本时：

1. 只从生产 Vault 之外的路径复制音频到 `.local-test-data/audio/`。
2. 在本地 `manifest.json` 中增加条目，并记录 `shasum -a 256`、文件字节数和 `ffprobe` 时长。
3. 使用稳定的英文 ID，通过标签说明格式、文件名特征、Provider 和回归问题。
4. 运行 `npm run verify:audio-dataset`，确认哈希、媒体元数据和 Git 忽略状态全部通过。

可用环境变量 `ECHO_NOTES_AUDIO_DATASET_DIR` 把数据集放到其他非生产 Vault 路径。默认保护 `~/笔记/lifeos-obsidian`；如生产 Vault 位于其他路径，用 `ECHO_NOTES_PROTECTED_VAULT_DIRS` 按系统路径分隔符配置一个或多个受保护目录。加载器拒绝符号链接、目录越界和受保护 Vault 内的路径。
